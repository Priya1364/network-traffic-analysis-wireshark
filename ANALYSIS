Analysis:

1. DNS Traffic:
When accessing a website, DNS query is generated to resolve domain name into IP address.

2. HTTP Traffic:
HTTP packets contain readable data, which shows that it is not secure.

3. TCP Communication:
TCP establishes connection using SYN, SYN-ACK, ACK process.

Observation:
When I searched "google.com", I observed DNS query packets followed by HTTP requests.

Inference:
This shows that DNS resolution happens before accessing a website.

small process diagram :
User → DNS Query → IP Address → HTTP Request → Website Response
